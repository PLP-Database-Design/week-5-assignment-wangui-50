<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Providers List</title>
</head>
<body>
    <h1>Providers List</h1>
    <table>
        <thead>
            <tr>
                <th>First Name</th>
                <th>Last Name</th>
                <th>Provider Specialty</th>
            </tr>
        </thead>
        <tbody>
            <% if (results.length === 0) { %>
                <tr>
                    <td colspan="3">No providers found for this specialty.</td>
                </tr>
            <% } else { %>
                <% results.forEach(row => { %>
                    <tr>
                        <td><%= row.first_name %></td>
                        <td><%= row.last_name %></td>
                        <td><%= row.provider_specialty %></td>
                    </tr>
                <% }); %>
            <% } %>
        </tbody>
    </table>
</body>
</html>
